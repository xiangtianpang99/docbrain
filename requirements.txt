chromadb             # 向量数据库，用于存储文档嵌入
sentence-transformers # 本地嵌入模型引擎，将文本转换为向量
langchain            # LLM 应用开发框架
langchain-community  # LangChain 社区标准扩展包
langchain-text-splitters # 专门用于文档切分的工具
langchain-core       # LangChain 核心抽象接口
openai               # OpenAI API 客户端（DeepSeek 兼容此接口）
python-dotenv        # 读取 .env 环境变量文件
watchdog             # 实时监控本地文件变动
pypdf                # 解析 PDF 文件
python-docx          # 解析 Word (.docx) 文件
openpyxl             # 解析 Excel (.xlsx) 文件
python-pptx          # 解析 PPT (.pptx) 文件
fastapi              # 高性能 Web API 框架
uvicorn              # FastAPI 的运行服务器
markdownify          # 将网页内容转换为 Markdown
crewai               # 多智能体协同框架（核心逻辑）
crewai-tools         # CrewAI 的标准工具包
litellm              # 多模型统一调用接口，支持 DeepSeek
pandas               # 强大的数据处理库，Excel 分析必备
xlrd                 # [新] 支持旧版 Excel (.xls)
unstructured[local-inference] # [新] 支持旧版 Word (.doc) 和 PPT (.ppt)
